根据这个 PreferencesModal 的内容，生成全屏弹窗的 PreferencesPage：

- 保存为 src/pages/PreferencesPage.vue。
- 顶栏使用 UniversalTopbar。
- 把 PreferencesModal 里的选项分组，在 PreferencesPage 里改为用 iOS 风格的分组，即：
    - 每一个选项分组，使用 iOS 风格的分组
    - 分组里的每一项，独占一行